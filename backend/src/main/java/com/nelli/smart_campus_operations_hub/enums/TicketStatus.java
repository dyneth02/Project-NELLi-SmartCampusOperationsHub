package com.nelli.smart_campus_operations_hub.enums;

/**
 * Represents the workflow states of a maintenance tickets.
 */
public enum TicketStatus {
    OPEN,
    IN_PROGRESS,
    RESOLVED,
    CLOSED,
    REJECTED
}
